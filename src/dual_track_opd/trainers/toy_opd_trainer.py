"""CPU-only toy OPD trainer placeholder."""

from __future__ import annotations


def run_toy_training(max_steps: int = 1) -> dict[str, int]:
    """Return a deterministic placeholder training summary."""

    return {"steps": max_steps}

